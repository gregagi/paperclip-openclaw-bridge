# Changesets

Add a changeset file in any PR that should eventually ship to npm.

Typical flow:

1. Run `npx changeset`
2. Choose the package
3. Pick `patch`, `minor`, or `major`
4. Write a short user-facing summary
5. Commit the generated markdown file with your code changes
